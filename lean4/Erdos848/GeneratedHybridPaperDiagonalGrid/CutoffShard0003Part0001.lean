import Erdos848.GeneratedHybridPaperDiagonalGrid.CutoffShard0003Data
import Erdos848.GeneratedHybridPaperDiagonalGrid.RowBlock0008
import Erdos848.GeneratedHybridPaperDiagonalGrid.RowBlock0009
import Erdos848.GeneratedHybridPaperDiagonalGrid.RowBlock0010
import Erdos848.GeneratedHybridPaperDiagonalGrid.RowBlock0011
import Erdos848.GeneratedHybridPaperDiagonalGrid.RowBlock0012
import Erdos848.GeneratedHybridPaperDiagonalGrid.RowBlock0013
import Erdos848.GeneratedHybridPaperDiagonalGrid.RowBlock0014
import Erdos848.GeneratedHybridPaperDiagonalGrid.RowBlock0015

namespace Erdos848.GeneratedHybridPaperDiagonalGrid

set_option maxRecDepth 1000000
set_option maxHeartbeats 0

theorem cutoffShard0003Row0128
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0128.upper
      (row0128.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0129
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0129.upper
      (row0129.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0130
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0130.upper
      (row0130.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0131
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0131.upper
      (row0131.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0132
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0132.upper
      (row0132.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0133
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0133.upper
      (row0133.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0134
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0134.upper
      (row0134.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0135
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0135.upper
      (row0135.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0136
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0136.upper
      (row0136.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0137
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0137.upper
      (row0137.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0138
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0138.upper
      (row0138.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0139
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0139.upper
      (row0139.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0140
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0140.upper
      (row0140.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0141
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0141.upper
      (row0141.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0142
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0142.upper
      (row0142.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0143
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0143.upper
      (row0143.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0144
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0144.upper
      (row0144.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0145
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0145.upper
      (row0145.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0146
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0146.upper
      (row0146.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0147
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0147.upper
      (row0147.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0148
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0148.upper
      (row0148.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0149
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0149.upper
      (row0149.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0150
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0150.upper
      (row0150.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0151
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0151.upper
      (row0151.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0152
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0152.upper
      (row0152.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0153
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0153.upper
      (row0153.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0154
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0154.upper
      (row0154.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0155
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0155.upper
      (row0155.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0156
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0156.upper
      (row0156.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0157
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0157.upper
      (row0157.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0158
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0158.upper
      (row0158.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0159
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0159.upper
      (row0159.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0160
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0160.upper
      (row0160.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0161
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0161.upper
      (row0161.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0162
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0162.upper
      (row0162.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0163
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0163.upper
      (row0163.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0164
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0164.upper
      (row0164.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0165
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0165.upper
      (row0165.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0166
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0166.upper
      (row0166.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0167
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0167.upper
      (row0167.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0168
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0168.upper
      (row0168.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0169
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0169.upper
      (row0169.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0170
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0170.upper
      (row0170.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0171
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0171.upper
      (row0171.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0172
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0172.upper
      (row0172.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0173
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0173.upper
      (row0173.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0174
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0174.upper
      (row0174.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0175
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0175.upper
      (row0175.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0176
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0176.upper
      (row0176.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0177
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0177.upper
      (row0177.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0178
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0178.upper
      (row0178.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0179
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0179.upper
      (row0179.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0180
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0180.upper
      (row0180.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0181
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0181.upper
      (row0181.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0182
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0182.upper
      (row0182.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0183
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0183.upper
      (row0183.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0184
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0184.upper
      (row0184.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0185
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0185.upper
      (row0185.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0186
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0186.upper
      (row0186.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0187
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0187.upper
      (row0187.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0188
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0188.upper
      (row0188.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0189
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0189.upper
      (row0189.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0190
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0190.upper
      (row0190.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0191
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0191.upper
      (row0191.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0192
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0192.upper
      (row0192.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0193
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0193.upper
      (row0193.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0194
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0194.upper
      (row0194.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0195
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0195.upper
      (row0195.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0196
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0196.upper
      (row0196.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0197
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0197.upper
      (row0197.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0198
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0198.upper
      (row0198.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0199
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0199.upper
      (row0199.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0200
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0200.upper
      (row0200.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0201
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0201.upper
      (row0201.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0202
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0202.upper
      (row0202.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0203
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0203.upper
      (row0203.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0204
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0204.upper
      (row0204.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0205
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0205.upper
      (row0205.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0206
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0206.upper
      (row0206.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0207
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0207.upper
      (row0207.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0208
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0208.upper
      (row0208.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0209
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0209.upper
      (row0209.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0210
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0210.upper
      (row0210.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0211
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0211.upper
      (row0211.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0212
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0212.upper
      (row0212.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0213
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0213.upper
      (row0213.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0214
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0214.upper
      (row0214.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0215
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0215.upper
      (row0215.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0216
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0216.upper
      (row0216.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0217
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0217.upper
      (row0217.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0218
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0218.upper
      (row0218.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0219
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0219.upper
      (row0219.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0220
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0220.upper
      (row0220.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0221
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0221.upper
      (row0221.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0222
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0222.upper
      (row0222.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0223
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0223.upper
      (row0223.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0224
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0224.upper
      (row0224.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0225
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0225.upper
      (row0225.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0226
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0226.upper
      (row0226.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0227
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0227.upper
      (row0227.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0228
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0228.upper
      (row0228.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0229
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0229.upper
      (row0229.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0230
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0230.upper
      (row0230.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0231
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0231.upper
      (row0231.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0232
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0232.upper
      (row0232.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0233
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0233.upper
      (row0233.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0234
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0234.upper
      (row0234.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0235
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0235.upper
      (row0235.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0236
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0236.upper
      (row0236.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0237
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0237.upper
      (row0237.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0238
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0238.upper
      (row0238.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0239
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0239.upper
      (row0239.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0240
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0240.upper
      (row0240.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0241
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0241.upper
      (row0241.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0242
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0242.upper
      (row0242.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0243
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0243.upper
      (row0243.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0244
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0244.upper
      (row0244.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0245
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0245.upper
      (row0245.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0246
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0246.upper
      (row0246.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0247
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0247.upper
      (row0247.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0248
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0248.upper
      (row0248.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0249
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0249.upper
      (row0249.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0250
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0250.upper
      (row0250.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0251
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0251.upper
      (row0251.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0252
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0252.upper
      (row0252.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0253
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0253.upper
      (row0253.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0254
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0254.upper
      (row0254.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0003Row0255
    (j : Fin 63) :
    (cutoffShard0003Targets j).Cutoff
      row0255.upper
      (row0255.counts.count (cutoffShard0003Atoms j)) := by
  fin_cases j <;> decide

end Erdos848.GeneratedHybridPaperDiagonalGrid
