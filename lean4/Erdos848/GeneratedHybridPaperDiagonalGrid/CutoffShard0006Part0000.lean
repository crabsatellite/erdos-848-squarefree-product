import Erdos848.GeneratedHybridPaperDiagonalGrid.CutoffShard0006Data
import Erdos848.GeneratedHybridPaperDiagonalGrid.RowBlock0000
import Erdos848.GeneratedHybridPaperDiagonalGrid.RowBlock0001
import Erdos848.GeneratedHybridPaperDiagonalGrid.RowBlock0002
import Erdos848.GeneratedHybridPaperDiagonalGrid.RowBlock0003
import Erdos848.GeneratedHybridPaperDiagonalGrid.RowBlock0004
import Erdos848.GeneratedHybridPaperDiagonalGrid.RowBlock0005
import Erdos848.GeneratedHybridPaperDiagonalGrid.RowBlock0006
import Erdos848.GeneratedHybridPaperDiagonalGrid.RowBlock0007

namespace Erdos848.GeneratedHybridPaperDiagonalGrid

set_option maxRecDepth 1000000
set_option maxHeartbeats 0

theorem cutoffShard0006Row0000
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0000.upper
      (row0000.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0001
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0001.upper
      (row0001.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0002
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0002.upper
      (row0002.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0003
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0003.upper
      (row0003.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0004
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0004.upper
      (row0004.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0005
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0005.upper
      (row0005.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0006
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0006.upper
      (row0006.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0007
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0007.upper
      (row0007.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0008
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0008.upper
      (row0008.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0009
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0009.upper
      (row0009.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0010
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0010.upper
      (row0010.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0011
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0011.upper
      (row0011.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0012
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0012.upper
      (row0012.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0013
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0013.upper
      (row0013.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0014
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0014.upper
      (row0014.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0015
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0015.upper
      (row0015.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0016
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0016.upper
      (row0016.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0017
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0017.upper
      (row0017.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0018
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0018.upper
      (row0018.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0019
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0019.upper
      (row0019.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0020
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0020.upper
      (row0020.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0021
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0021.upper
      (row0021.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0022
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0022.upper
      (row0022.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0023
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0023.upper
      (row0023.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0024
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0024.upper
      (row0024.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0025
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0025.upper
      (row0025.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0026
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0026.upper
      (row0026.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0027
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0027.upper
      (row0027.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0028
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0028.upper
      (row0028.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0029
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0029.upper
      (row0029.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0030
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0030.upper
      (row0030.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0031
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0031.upper
      (row0031.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0032
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0032.upper
      (row0032.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0033
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0033.upper
      (row0033.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0034
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0034.upper
      (row0034.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0035
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0035.upper
      (row0035.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0036
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0036.upper
      (row0036.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0037
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0037.upper
      (row0037.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0038
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0038.upper
      (row0038.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0039
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0039.upper
      (row0039.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0040
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0040.upper
      (row0040.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0041
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0041.upper
      (row0041.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0042
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0042.upper
      (row0042.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0043
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0043.upper
      (row0043.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0044
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0044.upper
      (row0044.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0045
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0045.upper
      (row0045.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0046
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0046.upper
      (row0046.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0047
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0047.upper
      (row0047.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0048
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0048.upper
      (row0048.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0049
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0049.upper
      (row0049.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0050
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0050.upper
      (row0050.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0051
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0051.upper
      (row0051.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0052
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0052.upper
      (row0052.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0053
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0053.upper
      (row0053.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0054
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0054.upper
      (row0054.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0055
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0055.upper
      (row0055.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0056
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0056.upper
      (row0056.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0057
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0057.upper
      (row0057.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0058
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0058.upper
      (row0058.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0059
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0059.upper
      (row0059.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0060
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0060.upper
      (row0060.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0061
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0061.upper
      (row0061.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0062
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0062.upper
      (row0062.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0063
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0063.upper
      (row0063.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0064
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0064.upper
      (row0064.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0065
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0065.upper
      (row0065.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0066
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0066.upper
      (row0066.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0067
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0067.upper
      (row0067.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0068
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0068.upper
      (row0068.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0069
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0069.upper
      (row0069.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0070
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0070.upper
      (row0070.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0071
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0071.upper
      (row0071.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0072
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0072.upper
      (row0072.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0073
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0073.upper
      (row0073.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0074
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0074.upper
      (row0074.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0075
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0075.upper
      (row0075.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0076
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0076.upper
      (row0076.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0077
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0077.upper
      (row0077.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0078
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0078.upper
      (row0078.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0079
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0079.upper
      (row0079.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0080
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0080.upper
      (row0080.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0081
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0081.upper
      (row0081.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0082
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0082.upper
      (row0082.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0083
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0083.upper
      (row0083.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0084
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0084.upper
      (row0084.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0085
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0085.upper
      (row0085.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0086
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0086.upper
      (row0086.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0087
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0087.upper
      (row0087.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0088
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0088.upper
      (row0088.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0089
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0089.upper
      (row0089.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0090
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0090.upper
      (row0090.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0091
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0091.upper
      (row0091.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0092
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0092.upper
      (row0092.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0093
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0093.upper
      (row0093.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0094
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0094.upper
      (row0094.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0095
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0095.upper
      (row0095.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0096
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0096.upper
      (row0096.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0097
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0097.upper
      (row0097.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0098
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0098.upper
      (row0098.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0099
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0099.upper
      (row0099.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0100
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0100.upper
      (row0100.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0101
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0101.upper
      (row0101.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0102
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0102.upper
      (row0102.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0103
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0103.upper
      (row0103.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0104
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0104.upper
      (row0104.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0105
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0105.upper
      (row0105.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0106
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0106.upper
      (row0106.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0107
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0107.upper
      (row0107.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0108
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0108.upper
      (row0108.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0109
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0109.upper
      (row0109.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0110
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0110.upper
      (row0110.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0111
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0111.upper
      (row0111.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0112
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0112.upper
      (row0112.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0113
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0113.upper
      (row0113.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0114
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0114.upper
      (row0114.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0115
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0115.upper
      (row0115.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0116
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0116.upper
      (row0116.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0117
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0117.upper
      (row0117.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0118
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0118.upper
      (row0118.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0119
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0119.upper
      (row0119.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0120
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0120.upper
      (row0120.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0121
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0121.upper
      (row0121.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0122
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0122.upper
      (row0122.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0123
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0123.upper
      (row0123.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0124
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0124.upper
      (row0124.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0125
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0125.upper
      (row0125.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0126
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0126.upper
      (row0126.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

theorem cutoffShard0006Row0127
    (j : Fin 26) :
    (cutoffShard0006Targets j).Cutoff
      row0127.upper
      (row0127.counts.count (cutoffShard0006Atoms j)) := by
  fin_cases j <;> decide

end Erdos848.GeneratedHybridPaperDiagonalGrid
